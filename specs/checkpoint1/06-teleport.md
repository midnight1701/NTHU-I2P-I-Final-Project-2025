# Teleport

Score: 1 point

- [ ] Player can enter a building by walking into certain tiles defined inside saves/*.json, and the map will be changed

Hint: There is an API to switch the map inside src/core/managers/game_manager.py called `switch_map`
